# Replaces Omarchy's generated hyprlock.conf. The generated file stops at the
# five variables Omarchy's stock lock screen uses; the extra ones below are for
# the layout in ~/.config/hypr/hyprlock.conf.

$color = rgba(24, 23, 22, 1.0)
$inner_color = rgba(24, 23, 22, 0.6)
$outer_color = rgba(255, 140, 0, 0.55)
$font_color = rgba(240, 227, 213, 1.0)
$check_color = rgba(138, 157, 51, 0.9)

# The password box on the boot splash and the login screen is a bitmap,
# entry.png, tinted with this theme's foreground by omarchy-plymouth-set. These
# are the alphas measured off it, so the lock screen draws the same box.
$entry_border = rgba(240, 227, 213, 0.58)
$entry_fill = rgba(240, 227, 213, 0.05)

$accent_color = rgba(255, 140, 0, 1.0)
$accent_soft = rgba(255, 140, 0, 0.5)
$accent_faint = rgba(255, 140, 0, 0.05)
$subtle_color = rgba(240, 227, 213, 0.45)
$hint_color = rgba(240, 227, 213, 0.25)
$fail_color = rgba(255, 50, 0, 0.9)
$fail_soft = rgba(255, 50, 0, 0.15)
$caps_color = rgba(255, 196, 112, 0.9)
